// mod satellites;
// use satellites::run;

// mod floating_points;
// use floating_points::run;

// mod fixed_point_formats;
// use fixed_point_formats::run;

// mod mock_randomness;
// use mock_randomness::run;

// mod cpu;
// use cpu::run;

mod snow;
use snow::run;

fn main() {
    // run();
    run();
}
